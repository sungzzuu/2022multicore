독같은 input이 잇으면 같은 결과가 나와야 한다. 대부분의 자료구조는 결정적이다.
자료구조의 메소드를 호출했을때 결과가 다른건 거의 없음.
=> 거의 모든 자료구조를 바꿀 수 있다!
같은 입력 값을 동일한 순서로 입력!

오퍼레이션의 아이디를 넣고 파라미터도 넣는다. invocation -> 어떤 메소드인가? 
모든 파라미터의 조합을 다 넣어주어서 해당하는 메소드의 파라미터를 거낼 수 있게 한다.
여러 종류의 리턴값을 하나로 묶은게 response

class Response
{
	int x;
	int y;
	int xz;
	...
}
큐를 변형해서 apply라는 메소드 하나로 사용하게함



